# Copy to .env for local development. Never commit .env — it is gitignored.
#
# Only VITE_-prefixed names are exposed to the client bundle via import.meta.env.
# An unprefixed name is still loaded by Vite but never reaches the browser, so the
# URL shortener silently falls back to unshortened URLs (see js/juiceboxConfig.js).
#
# The same name must also be set on the Cloudflare Pages project (juicebox-web),
# for both the production and preview environments, or hosted builds lose shortening.

# TinyURL API key for URL shortening. Get one at https://tinyurl.com/app/dev
VITE_TINYURL_JUICEBOX_API_KEY=
